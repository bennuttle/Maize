using UnityEngine;
using System.Collections;

public class testyolo : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log (Application.persistentDataPath);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
